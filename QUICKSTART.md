# 🚀 Быстрый старт

## 📦 Установка за 3 шага

### Шаг 1: Установите зависимости

**Windows:**
```bash
# Двойной клик по файлу
install.bat

# Или через PowerShell/CMD
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip install -r requirements.txt
```

### Шаг 2: Настройте переменные окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
# Скопируйте шаблон
copy .env.example .env  # Windows
# или
cp .env.example .env    # Linux/Mac
```

Откройте `.env` и укажите ваши API ключи:

```env
BOT_TOKEN=ваш_токен_telegram_бота
CALORIE_NINJAS_API_KEY=ваш_ключ_api
```

✅ Если файл `.env` уже существует с вашими ключами - пропустите этот шаг!

### Шаг 3: Запустите бота

**Windows:**
```bash
# Двойной клик по файлу
start_bot.bat

# Или через PowerShell/CMD
python main.py
```

**Linux/Mac:**
```bash
python main.py
```

## ✅ Проверка работы

1. Откройте Telegram
2. Найдите вашего бота: `@AI_buter_bot`
3. Отправьте команду `/start`
4. Увидите главное меню с кнопками

## 🎯 Первое использование

### Поиск рецепта:
1. Нажмите "🍳 Найти рецепт"
2. Введите: `chicken pasta`
3. Получите несколько рецептов

### Подсчет калорий:
1. Нажмите "📊 Подсчитать калории"
2. Выберите "✍️ Ввести текстом"
3. Введите: `200g chicken breast, 100g rice`
4. Получите информацию о калориях и БЖУ

## 🐛 Troubleshooting

### Ошибка: "Unauthorized" или "BOT_TOKEN не найден"
- Проверьте наличие файла `.env` в корне проекта
- Убедитесь, что `BOT_TOKEN` указан в `.env` файле
- Проверьте, что токен активен в @BotFather

### Ошибка: "API key invalid" или "CALORIE_NINJAS_API_KEY не найден"
- Проверьте наличие файла `.env` в корне проекта
- Убедитесь, что `CALORIE_NINJAS_API_KEY` указан в `.env` файле
- Проверьте, что ключ не истек

### Ошибка: "Module not found"
- Запустите: `pip install -r requirements.txt`
- Убедитесь, что используете правильный Python (3.8+)

### Бот не отвечает
- Проверьте интернет соединение
- Убедитесь, что бот запущен (должно быть сообщение "🚀 Бот запущен и готов к работе!")
- Проверьте логи в консоли

## 📱 Основные команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота |
| `/help` | Получить справку |
| `🍳 Найти рецепт` | Поиск рецептов |
| `📊 Подсчитать калории` | Анализ калорий |
| `◀️ Назад` | Вернуться в меню |

## 🔧 Требования

- Python 3.8 или выше
- Интернет соединение
- Активный токен Telegram бота
- API ключ CalorieNinjas

## 📊 Проверка зависимостей

```bash
python --version  # Должно быть >= 3.8
pip list | grep aiogram  # Проверка aiogram
```

## 🎨 Примеры запросов

### Рецепты (работают лучше на английском):
- `pasta carbonara`
- `chocolate cake`
- `chicken soup`
- `vegan salad`
- `pizza margherita`

### Калории (работают на русском и английском):
- `200g куриная грудка, 100g рис`
- `apple 150g, banana`
- `300g pasta, 50g butter`
- `Борщ 400мл`
- `2 eggs, toast`

## 🌟 Полезные ссылки

- [aiogram документация](https://docs.aiogram.dev/)
- [CalorieNinjas API](https://calorieninjas.com/api)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## 💡 Советы

1. **Для точного подсчета калорий** всегда указывайте вес продуктов
2. **Для поиска рецептов** используйте конкретные названия блюд
3. **Английские запросы** работают лучше для рецептов
4. **Русские запросы** работают для подсчета калорий

## 🔄 Обновление бота

Если вы изменили код:
1. Остановите бота (Ctrl+C в консоли)
2. Сохраните изменения
3. Запустите снова: `python main.py`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте наличие файла `.env` с API ключами
4. Прочитайте `README.md` и `ARCHITECTURE.md`

---

**Готово! Ваш бот должен работать! 🎉**

Не забудьте прочитать `ARCHITECTURE.md` для понимания структуры проекта.

